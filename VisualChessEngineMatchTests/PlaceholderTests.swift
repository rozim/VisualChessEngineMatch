import XCTest

/// A trivial test that proves the test target is wired up and runnable.
/// Replaced by real model tests in later steps.
final class PlaceholderTests: XCTestCase {
    func testProjectBuildsAndTestsRun() {
        XCTAssertEqual(2 + 2, 4)
    }
}
